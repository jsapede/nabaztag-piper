type: vertical-stack
cards:
  # ========== HEADER ==========
  - type: markdown
    content: |
      # 🐇 **Nabaztag**
      État firmware interrogé par telnet toutes les 30s.
      {% set u = state_attr('sensor.nabaztag_telnet_status', 'uptime') | int(0) %}
      {% set d = u // 86400 %} {% set h = (u % 86400) // 3600 %} {% set m = (u % 3600) // 60 %}
      {% if u > 0 %}Uptime : **{{ d }}j {{ h }}h {{ m }}m**{% else %}🔄 Reboot en cours...{% endif %}

  # ========== SYNCHRONISATION ==========
  - type: markdown
    content: >
      ### 🔄 Synchronisation HA ↔ Lapin

      {% set ac = state_attr('sensor.nabaztag_telnet_status', 'autoclock') %}
      {% set ah = state_attr('sensor.nabaztag_telnet_status', 'autohalftime') %}
      {% set as = state_attr('sensor.nabaztag_telnet_status', 'autosurprise') %}
      {% set at = state_attr('sensor.nabaztag_telnet_status', 'autotaichi') %}
      {% set c = states('input_boolean.nabaztag_firmware_clock') == 'on' %}
      {% set h = states('input_boolean.nabaztag_firmware_halftime') == 'on' %}
      {% set s = states('input_boolean.nabaztag_firmware_surprise') == 'on' %}
      {% set t = states('input_boolean.nabaztag_firmware_taichi') == 'on' %}
      {% set sync = (c == (ac == 1)) and (h == (ah == 1)) and (s == (as == 1)) and (t == (at == 1)) %}
      **Configuration :** {{ '✅ Synchronisée' if sync else '❌ Désynchronisée' }}
      **Uptime :** {{ state_attr('sensor.nabaztag_telnet_status', 'uptime') | int(0) // 86400 }}j
      **Lapin :** {{ '💤 endormi' if state_attr('sensor.nabaztag_telnet_status', 'sleep_state') == 1 else '👀 éveillé' }}

  # ========== CONFIGURATION ==========
  - type: entities
    title: Configuration
    show_header_toggle: false
    state_color: false
    entities:
      - entity: input_select.nabaztag_language
        name: Langue
      - entity: input_select.nabaztag_timezone_city
        name: Ville / fuseau
      - entity: input_boolean.nabaztag_auto_dst
        name: Auto DST
      - entity: input_number.nabaztag_wake_hour
        name: Réveil heure
      - entity: input_number.nabaztag_sleep_hour
        name: Coucher heure
      - entity: input_number.nabaztag_taichi_freq
        name: Fréquence Taichi
      - entity: input_text.nabaztag_message
        name: Message à dire
      - type: button
        name: Dire le message
        icon: mdi:volume-high
        tap_action:
          action: call-service
          service: script.nabaztag_talk
          target: {}

  # ========== PARAMETRES ==========
  - type: markdown
    content: "## ⚙️ Paramètres"
  - type: grid
    columns: 2
    square: false
    cards:
      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_firmware_clock
        icon: mdi:clock-outline
        primary: Annonce heure
        color: |
          {% if is_state(entity, 'on') and state_attr('sensor.nabaztag_telnet_status', 'autoclock') == 1 %}
            green
          {% elif is_state(entity, 'on') %}
            orange
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_firmware_halftime
        icon: mdi:bell-ring-outline
        primary: Annonce 1/2 heure
        color: |
          {% if is_state(entity, 'on') and state_attr('sensor.nabaztag_telnet_status', 'autohalftime') == 1 %}
            green
          {% elif is_state(entity, 'on') %}
            orange
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_firmware_surprise
        icon: mdi:gift-outline
        primary: Sons surprise
        color: |
          {% if is_state(entity, 'on') and state_attr('sensor.nabaztag_telnet_status', 'autosurprise') == 1 %}
            green
          {% elif is_state(entity, 'on') %}
            orange
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_firmware_taichi
        icon: mdi:meditation
        primary: Anim Taichi
        color: |
          {% if is_state(entity, 'on') and state_attr('sensor.nabaztag_telnet_status', 'autotaichi') == 1 %}
            green
          {% elif is_state(entity, 'on') %}
            orange
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_weather_enabled
        icon: mdi:weather-partly-cloudy
        primary: LED météo
        color: |
          {% if is_state(entity, 'on') and state_attr('sensor.nabaztag_telnet_status', 'weather') not in [None, -1] %}
            green
          {% elif is_state(entity, 'on') %}
            orange
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_traffic_enabled
        icon: mdi:car
        primary: LED trafic
        color: |
          {% if is_state(entity, 'on') and state_attr('sensor.nabaztag_telnet_status', 'traffic') not in [None, -1] %}
            green
          {% elif is_state(entity, 'on') %}
            orange
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_pollution_enabled
        icon: mdi:factory
        primary: LED pollution
        color: |
          {% if is_state(entity, 'on') and state_attr('sensor.nabaztag_telnet_status', 'pollution') not in [None, -1] %}
            green
          {% elif is_state(entity, 'on') %}
            orange
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_nose_enabled
        icon: mdi:lightbulb-on-outline
        primary: LED Nez
        color: |
          {% if is_state(entity, 'on') %}
            green
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_traffic_auto_update
        icon: mdi:car-clock
        primary: Màj trafic auto HA
        color: |
          {% if is_state(entity, 'on') %}
            green
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_weather_auto_update
        icon: mdi:weather-sync
        primary: Màj météo auto HA
        color: |
          {% if is_state(entity, 'on') %}
            green
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

      - type: custom:mushroom-template-card
        entity: input_boolean.nabaztag_pollution_auto_update
        icon: mdi:air-filter
        primary: Màj pollution auto HA
        color: |
          {% if is_state(entity, 'on') %}
            green
          {% else %}
            disabled
          {% endif %}
        tap_action:
          action: toggle
        vertical: false

